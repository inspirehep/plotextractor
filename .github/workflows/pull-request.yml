name: Pull request master

on:
  pull_request:
    branches: [master]

jobs:
  lint:
    uses: ./.github/workflows/lint.yml
    with:
      ref: ${{ github.sha }}

  python3_tests:
    needs: [lint]
    uses: ./.github/workflows/test-python-3.yml
